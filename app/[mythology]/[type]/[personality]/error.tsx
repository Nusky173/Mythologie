export default function PersonalityError({
    // params,
    // } : {
    //     params : {mythology: string, type: string, personality: string}
    }) {
        return <div>    
            {/* Error 404 : Le {params.type} {params.personality} n'existe pas pour la mythology {params.mythology},  */}
        </div>
}